# راه‌اندازی روی وردپرس — راهنمای گام‌به‌گام

این پروژه شامل قالب اختصاصی وردپرس **کاتالیزور** است: `theme/catalyzer/`.

---

## ۱) پیش‌نیازها روی هاست

- وردپرس ۶٫۲ به بالا، PHP ۷٫۴ به بالا
- زبان سایت: **فارسی** (تنظیمات → همگانی → زبان سایت)
- گواهی SSL فعال

## ۲) نصب قالب

**روش الف — بارگذاری zip (ساده):**

1. از پوشه‌ی `theme/catalyzer/` یک فایل zip بسازید (نامش مثلاً `catalyzer.zip`).
   - ویندوز/پاورشل:
     ```powershell
     Compress-Archive -Path "theme/catalyzer/*" -DestinationPath "catalyzer.zip"
     ```
     سپس داخل zip باید مستقیماً `style.css` و `functions.php` باشد (نه یک پوشه‌ی اضافه).
     اگر ابزار zip شما پوشه‌ی والد اضافه می‌کند، به‌جایش کل پوشه‌ی `catalyzer` را zip کنید.
2. پیشخوان → **نمایش → پوسته‌ها → افزودن → بارگذاری پوسته** → فایل zip → **هم‌اکنون نصب کن** → **فعال کردن**.

**روش ب — FTP/SFTP:**

پوشه‌ی `theme/catalyzer/` را کامل در مسیر `wp-content/themes/` هاست کپی کنید،
سپس از پیشخوان قالب «کاتالیزور» را فعال کنید.

## ۳) تنظیمات اولیه (یک‌بار)

1. **تنظیمات → پیوندهای یکتا** → روی «ذخیره» بزنید (بدون تغییر) تا آدرس دوره‌ها و ویدیوها کار کند.
2. **تنظیمات → خواندن** → «صفحه‌ی اصلی سایت شما نمایش دهد»:
   - گزینه‌ی مطمئن: یک برگه‌ی خالی به نام «خانه» بسازید و این‌جا انتخابش کنید.
   - (اگر «آخرین نوشته‌ها» را هم بگذارید، باز صفحه‌ی فرود نمایش داده می‌شود.)
3. **نمایش → سفارشی‌سازی → هویت سایت** → آپلود **لوگو**.

## ۴) وارد کردن محتوا

### متن‌ها و تصاویر صفحه‌ی فرود
**نمایش → سفارشی‌سازی → «کاتالیزور — محتوای صفحه‌ی فرود»**
همه‌ی بخش‌ها آن‌جا هستند: هیرو، نوار آمار، درباره/رزومه، متد کاتالیزور،
سربرگ بخش‌ها، بنر فراخوان، اطلاعات تماس، فوتر، و کلید روشن/خاموش هر بخش.

> در تیتر هیرو هر چیزی داخل `[ ]` بگذارید، کهربایی و برجسته می‌شود.
> در «رزومه»، هر خط یک مورد است؛ قالب: `بخش پررنگ | ادامه‌ی توضیح`.

### دوره‌ها و پکیج‌ها
پیشخوان → **دوره‌ها → افزودن دوره**. برای هر دوره:
عنوان، توضیح، تصویر شاخص، و در جعبه‌ی «مشخصات دوره»:
زیرعنوان، قیمت، قیمت پیشین (اختیاری)، ویژگی‌ها (هر خط یکی)، برچسب گوشه،
تیک «کارت ویژه»، متن و **لینک دکمه**.
سه دوره‌ی اول (بر اساس فیلد «ترتیب») در صفحه‌ی اصلی می‌آید.

### ویدیوهای کلاس
پیشخوان → **ویدیوهای کلاس → افزودن ویدیو**.
عنوان، تصویر شاخص (بندانگشتی)، و در جعبه‌ی «مشخصات ویدیو»:
**آدرس ویدیو** (لینک یوتیوب یا اپارات یا فایل mp4)، مدت زمان، برچسب دسته، تیک «نمونه‌ی رایگان».

### نظرات دانش‌آموزان
پیشخوان → **نظرات → افزودن نظر**. عنوان = خط معرفی، متن = نقل‌قول.

## ۵) فروش و پرداخت (ووکامرس)

1. افزونه‌ی **WooCommerce** را نصب و راه‌اندازی کنید.
2. یک درگاه پرداخت ایرانی نصب کنید (مثلاً افزونه‌ی «درگاه پرداخت زرین‌پال برای ووکامرس»).
3. برای هر پکیج یک **محصول** بسازید (نوع مجازی/دانلودی، قیمت واقعی).
4. در دوره‌ی متناظر → «لینک دکمه» را روی آدرس آن محصول بگذارید.

حالا کاربر از صفحه‌ی فرود روی «ثبت‌نام» می‌زند و مستقیم به صفحه‌ی خرید و پرداخت می‌رود.

## ۶) فرم تماس

- فرم داخلی آماده است؛ پیام‌ها در **پیشخوان → درخواست‌های مشاوره** ذخیره و به ایمیل مدیر ارسال می‌شوند.
- ایمیل مقصد را در سفارشی‌سازی → تماس تغییر دهید. برای تحویل مطمئنِ ایمیل، افزونه‌ی SMTP (مثل WP Mail SMTP) نصب کنید.
- اگر Contact Form 7 را ترجیح می‌دهید: شورت‌کد فرم را در سفارشی‌سازی → تماس بگذارید.

## ۷) بهینه‌سازی پیشنهادی

| کار | افزونه‌ی پیشنهادی |
| --- | --- |
| کش و فشرده‌سازی | LiteSpeed Cache / WP Rocket |
| میزبانی محلی فونت گوگل | OMGF |
| بهینه‌سازی تصاویر | ShortPixel / Imagify |
| امنیت و پشتیبان | Wordfence + UpdraftPlus |
| SMTP ایمیل | WP Mail SMTP |

## ۸) از این نمونه‌ی HTML به قالب

- `index.html` (در ریشه‌ی مخزن) نسخه‌ی استاتیک است و برای مرجع طراحی نگه داشته شده.
- منطق و ظاهر آن عیناً داخل `theme/catalyzer/` پیاده شده؛ برای انتشار فقط پوشه‌ی قالب لازم است.
